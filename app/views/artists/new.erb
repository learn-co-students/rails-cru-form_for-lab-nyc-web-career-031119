<h1>New Artist Form</h1>

<%= form_for Artist.new do |f| %>
  <%= f.label :name %><br>
  <%= f.text_field :name %><br>

  <%= f.label :bio %><br>
  <%= f.text_area :bio %><br>

  <%= f.submit %>
<% end %>
